apiVersion: v1
kind: Service
metadata:
  name: image-service
  annotations:
    prometheus.io/port: "http"
    prometheus.io/scrape: "true"
  labels:
    app: image-service
spec:
  selector:
    app: image-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3002
