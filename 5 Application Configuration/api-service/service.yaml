apiVersion: v1
kind: Service
metadata:
  name: api-service
  annotations:
    prometheus.io/port: "http"
    prometheus.io/scrape: "true"
  labels:
    app: api-service
spec:
  selector:
    app: api-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
